<div class="mt-4">Show {{album.name}}</div>
<div class="row">
  <div class="col-sm-4 col-lg-3">
    <div class="card" style="width: 18rem;">
      <a href="/admin/album/{{album.slug}}">
        <img src="/img/{{album.img}}" class="card-img-top" alt="..." />
      </a>
      <div class="card-body">
        <h5 class="card-title">{{album.name}}</h5>
        <p class="card-text">Singer: {{album.singer}}</p>
        <a href="/admin/album/{{album.slug}}" class="btn btn-primary">Discover</a>
      </div>
    </div>
  </div>

</div>